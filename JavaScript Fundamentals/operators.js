/*
    1. ()
    2. * / %
    3. + -
    4. === !== >= <= > <
    5. || &&
    6. =
*/

let formula = ((4 + 5) / 2 - 10 / 5) * 3;
            // (9 / 2 - 10 / 5) * 3
            // (4.5 - 2) * 3
            // 2.5 * 3
            // 7.5
console.log(formula);